let base = 10;
let altura = 12;



const triangleArea = (base, altura) => base * altura / 2

console.log(`El area del triangulo de
base ${base}  y altura ${ altura } es: ${triangleArea(base, altura)}`) 

